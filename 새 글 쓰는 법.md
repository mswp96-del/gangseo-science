# 새 글 쓰는 법

## 전체 흐름

글은 **내 컴퓨터의 `posts` 폴더**에 있습니다. 여기에 글을 써도 인터넷 블로그에는 아직 안 보입니다.
`블로그 올리기.bat`을 더블클릭하면 그때 인터넷으로 복사되어 블로그에 나타납니다.

```
내 컴퓨터에서 글 쓰기  →  블로그 올리기.bat 더블클릭  →  1분 뒤 블로그에 반영
```

**할 일은 두 가지뿐입니다: `posts` 폴더에 글 파일 만들기, 그리고 `블로그 올리기.bat` 더블클릭.**

---

## 1단계 — 글 파일 만들기

`posts` 폴더를 열고, 메모장으로 새 파일을 만듭니다.
파일 이름은 **날짜-제목.md** 형식으로 짓습니다.

```
2026-09-01-빛의-굴절.md
```

## 2단계 — 글 쓰기

파일 맨 위에 아래 여섯 줄을 그대로 적습니다. `---` 두 줄은 꼭 있어야 합니다.

```
---
title: 빛의 굴절 실험
date: 2026-09-01
category: 수업자료
tags: 과학, 실험
summary: 목록에 보일 한 줄 설명
---
```

- **title** — 글 제목
- **date** — 날짜 (연-월-일)
- **category** — 분류. 블로그 위쪽 버튼으로 만들어집니다 (공지 / 수업자료 / 학급운영 등 자유롭게)
- **tags** — 쉼표로 구분
- **summary** — 목록에 보일 설명. 비워 두면 첫 문단이 자동으로 쓰입니다

그 아래에 본문을 씁니다. 그냥 줄글로 써도 되고, 아래 표시를 쓰면 꾸며집니다.

| 이렇게 쓰면 | 이렇게 보입니다 |
| --- | --- |
| `## 준비물` | 소제목 |
| `- 비커` | 점 목록 |
| `1. 물을 붓는다` | 번호 목록 |
| `**중요**` | **굵게** |
| `> 메모` | 인용 상자 |

## 3단계 — 올리기

블로그 폴더의 **`블로그 올리기.bat`을 더블클릭**합니다.
검은 창이 뜨고 "다 됐습니다!"가 나오면 끝입니다.

1분쯤 뒤 https://mswp96-del.github.io/gangseo-science/ 에서 확인할 수 있습니다.

---

## 올리기 전에 미리 보고 싶다면

`미리보기.bat`을 더블클릭하고, 브라우저에서 http://localhost:8321 로 들어갑니다.
확인이 끝나면 검은 창을 그냥 닫으면 됩니다.

## 사진 넣기

`assets` 폴더에 사진을 넣고, 본문에 이렇게 씁니다.

```
![실험 장면](assets/사진이름.jpg)
```

## 글 지우기

`posts` 폴더에서 파일을 지운 다음 `블로그 올리기.bat`을 실행하면 됩니다.

## posts/index.json 은 뭔가요?

블로그가 글 목록을 찾을 때 쓰는 파일인데, **선생님이 손댈 필요 없습니다.**
미리보기를 켜거나 올리기를 실행할 때 `posts` 폴더를 훑어서 자동으로 다시 만들어집니다.

---

## 잘 안 될 때

- **글이 안 보여요** → 파일 이름이 `.md`로 끝나는지, `posts` 폴더 안에 있는지 확인하세요. 맨 위 `---` 두 줄이 빠져도 제목이 이상하게 나옵니다.
- **"[실패]"가 떴어요** → 검은 창 내용을 클로드에게 그대로 보여주세요.
- **글자가 깨져요** → 메모장에서 저장할 때 인코딩을 **UTF-8**로 선택하세요.
