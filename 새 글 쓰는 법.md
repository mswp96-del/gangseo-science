# 새 글 쓰는 법

## 가장 쉬운 방법 — 글쓰기 앱

**`글쓰기.bat` 을 더블클릭**하면 브라우저에 글쓰기 화면이 열립니다.

제목·분류·본문을 채우고 **[인터넷에 올리기]** 를 누르면 파일 만들기, 목록 갱신, 올리기까지 한 번에 끝납니다.
검은 창은 글을 다 쓴 뒤에 닫으면 됩니다.

| 하고 싶은 것 | 하는 법 |
| --- | --- |
| 사진 넣기 | [🖼 사진 넣기] 버튼 (또는 본문 칸에 사진을 끌어다 놓기) — 여러 장 한꺼번에 됩니다 |
| 영상 넣기 | [🎬 영상 넣기] 버튼에 **유튜브 주소**를 붙여넣기. 짧은 실험 영상은 mp4 파일을 그대로 올려도 됩니다 |
| 올리기 전에 확인 | [미리보기] 버튼 |
| 컴퓨터에만 저장 | [저장만 하기] — 나중에 [인터넷에 올리기]를 누르면 그때 블로그에 올라갑니다 |
| 쓰던 글 이어 쓰기 | 창을 닫아도 자동으로 임시저장됩니다. 다시 열면 그대로 있습니다 |
| 올린 글 고치기·지우기 | 위쪽 **[📚 올린 글]** 탭 → [고치기] / [지우기] |

### 휴대폰에서 올리기

블로그 주소 뒤에 `write.html` 을 붙이면 (https://mswp96-del.github.io/gangseo-science/write.html)
휴대폰에서도 같은 화면이 열립니다. 처음 한 번만 GitHub 열쇠(토큰)를 등록하면
그 뒤로는 사진을 찍어 바로 올릴 수 있습니다. 열쇠는 그 기기 안에만 저장됩니다.

> 열쇠 만드는 법은 화면에 그대로 안내되어 있습니다.
> github.com → Settings → Developer settings → Fine-grained tokens → 이 저장소에 **Contents: Read and write** 권한만 주면 됩니다.

---

## 손으로 파일을 만들어 쓰는 방법

아래는 예전 방식입니다. 글쓰기 앱을 쓰면 안 해도 됩니다.

## 전체 흐름

글은 **내 컴퓨터의 `posts` 폴더**에 있습니다. 여기에 글을 써도 인터넷 블로그에는 아직 안 보입니다.
`블로그 올리기.bat`을 더블클릭하면 그때 인터넷으로 복사되어 블로그에 나타납니다.

```
내 컴퓨터에서 글 쓰기  →  블로그 올리기.bat 더블클릭  →  1분 뒤 블로그에 반영
```

**할 일은 두 가지뿐입니다: `posts` 폴더에 글 파일 만들기, 그리고 `블로그 올리기.bat` 더블클릭.**

---

## 1단계 — 글 파일 만들기

`posts` 폴더를 열고, 메모장으로 새 파일을 만듭니다.
파일 이름은 **날짜-제목.md** 형식으로 짓습니다.

```
2026-09-01-빛의-굴절.md
```

## 2단계 — 글 쓰기

파일 맨 위에 아래 여섯 줄을 그대로 적습니다. `---` 두 줄은 꼭 있어야 합니다.

```
---
title: 빛의 굴절 실험
date: 2026-09-01
category: 수업자료
tags: 과학, 실험
summary: 목록에 보일 한 줄 설명
---
```

- **title** — 글 제목
- **date** — 날짜 (연-월-일)
- **category** — 분류. 블로그 위쪽 버튼으로 만들어집니다 (공지 / 수업자료 / 학급운영 등 자유롭게)
- **tags** — 쉼표로 구분
- **summary** — 목록에 보일 설명. 비워 두면 첫 문단이 자동으로 쓰입니다

그 아래에 본문을 씁니다. 그냥 줄글로 써도 되고, 아래 표시를 쓰면 꾸며집니다.

| 이렇게 쓰면 | 이렇게 보입니다 |
| --- | --- |
| `## 준비물` | 소제목 |
| `- 비커` | 점 목록 |
| `1. 물을 붓는다` | 번호 목록 |
| `**중요**` | **굵게** |
| `> 메모` | 인용 상자 |

## 3단계 — 올리기

블로그 폴더의 **`블로그 올리기.bat`을 더블클릭**합니다.
검은 창이 뜨고 "다 됐습니다!"가 나오면 끝입니다.

1분쯤 뒤 https://mswp96-del.github.io/gangseo-science/ 에서 확인할 수 있습니다.

---

## 올리기 전에 미리 보고 싶다면

`미리보기.bat`을 더블클릭하고, 브라우저에서 http://localhost:8321 로 들어갑니다.
확인이 끝나면 검은 창을 그냥 닫으면 됩니다.

## 워드 파일로 글 만들기

이미 워드로 써 둔 문서가 있다면, **`워드 변환.bat` 아이콘 위로 워드 파일(.docx)을 끌어다 놓으세요.**

자동으로 처리되는 것:

- 제목, 소제목, 목록, 표를 마크다운으로 바꿈
- 문서 안의 **그림을 꺼내서** `assets` 폴더에 넣고 본문에 연결
- `posts` 폴더에 오늘 날짜로 파일 생성

만들어진 파일을 열어서 맨 위의 `date`(날짜)와 `summary`(요약)만 고치면 됩니다. 제목은 문서 첫 줄에서 자동으로 가져옵니다.

> 워드의 **수식과 도형**은 그림으로 삽입된 게 아니면 사라질 수 있습니다. 변환 후 `미리보기.bat`으로 꼭 확인하세요.

한글(.hwp) 파일은 지원하지 않습니다. 한글에서 "다른 이름으로 저장 → 워드 문서(.docx)"로 바꾼 뒤 사용하세요.

## PDF로 글 만들기

**`PDF 변환.bat` 아이콘 위로 PDF를 끌어다 놓으세요.** 각 쪽을 그림으로 만들어 슬라이드처럼 싣습니다.

| | 워드 변환.bat | PDF 변환.bat |
| --- | --- | --- |
| 넣는 것 | .docx | .pdf |
| 나오는 것 | 글자·표·그림이 살아 있는 글 | 쪽마다 그림 한 장 |
| 글자 검색 | 됩니다 | 안 됩니다 |

**PDF 안의 글자까지 살리고 싶다면** PDF를 Word로 열어(오른쪽 클릭 → 연결 프로그램 → Word) `.docx`로 저장한 뒤 `워드 변환.bat`을 쓰세요. 다만 스캔한 PDF나 발표 슬라이드처럼 원래 글자가 없는 PDF는 Word로 열어도 그림만 나옵니다. 그럴 때는 `PDF 변환.bat`이 훨씬 빠릅니다.

## 사진 넣기

`assets` 폴더에 사진을 넣고, 본문에 이렇게 씁니다.

```
![실험 장면](assets/사진이름.jpg)
```

## 영상 넣기

본문에 아래처럼 **주소만 한 줄** 적으면 재생기로 바뀝니다.

```
https://youtu.be/8xLPnfE1qNw
https://drive.google.com/file/d/파일아이디/view
assets/실험영상.mp4
```

유튜브·구글 드라이브·비메오 주소와 mp4 파일을 지원합니다.
큰 영상은 유튜브에 올린 뒤 주소만 넣는 쪽이 빠릅니다. (블로그에 직접 올리는 영상은 한 개 100MB까지)

## 글 지우기

`posts` 폴더에서 파일을 지운 다음 `블로그 올리기.bat`을 실행하면 됩니다.

## posts/index.json 은 뭔가요?

블로그가 글 목록을 찾을 때 쓰는 파일인데, **선생님이 손댈 필요 없습니다.**
미리보기를 켜거나 올리기를 실행할 때 `posts` 폴더를 훑어서 자동으로 다시 만들어집니다.

---

## 잘 안 될 때

- **글이 안 보여요** → 파일 이름이 `.md`로 끝나는지, `posts` 폴더 안에 있는지 확인하세요. 맨 위 `---` 두 줄이 빠져도 제목이 이상하게 나옵니다.
- **"[실패]"가 떴어요** → 검은 창 내용을 클로드에게 그대로 보여주세요.
- **글자가 깨져요** → 메모장에서 저장할 때 인코딩을 **UTF-8**로 선택하세요.
